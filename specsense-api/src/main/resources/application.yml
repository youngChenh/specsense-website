server:
  port: 8080
  servlet:
    context-path: /specsense
  tomcat:
    basedir: ./tomcat-temp

spring:
  application:
    name: specsense-api
  servlet:
    multipart:
      enabled: true
      file-size-threshold: 512KB
      max-file-size: 50MB
      max-request-size: 100MB
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://43.159.58.248/youthful_db?allowPublicKeyRetrieval=true&useUnicode=true&characterEncoding=utf8&useSSL=false&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true
    username: youthful
    password: Youthful@123456
  jackson:
    date-format: yyyy-MM-dd HH:mm:ss
    time-zone: Asia/Shanghai
    default-property-inclusion: non_null
  mail:
    host: smtp.gmail.com
    port: 587
    username: youngChenh@outlook.com
    password: Chc5930!
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
            required: true

mybatis:
  mapper-locations: classpath:mapper/*.xml
  type-aliases-package: com.specsense.model.entity
  configuration:
    map-underscore-to-camel-case: true
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl

# JWT Configuration
jwt:
  secret: SpecSenseSecretKey2024ForJWTTokenGeneration
  expiration: 86400000  # 24 hours in milliseconds

# File Upload Configuration
upload:
  path: ./uploads
  max-size: 100MB

# Logging
logging:
  level:
    com.specsense: DEBUG
    org.springframework: INFO
